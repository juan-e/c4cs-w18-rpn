language: python
rmv:
 - "3.5"

install: 
 - pip install python-coveralls
 - pip install coverage
 - pip install nose

script:
 - make test
 - pytest test_rpn.py --cov coveralls --cov-report

after_success:
 - coveralls
